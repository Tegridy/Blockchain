package blockchain;

import java.util.Scanner;

public class Client {

    private static Scanner scanner = new Scanner(System.in);

    public static void main(String[] args) {

        Blockchain blockchain = Blockchain.getBlockchain("blockchain.dat");

        while (scanner.hasNextLine()){
            blockchain.
        }


    }
}
